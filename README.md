


```markdown
# LVGL GUI Builder for EVM-ESP32 🎨

<div dir="rtl" align="right">

## 🇮🇷 بیلدر گرافیکی LVGL برای پروژه EVM-ESP32

یک ابزار طراحی رابط کاربری (GUI) با قابلیت کشیدن و رها کردن برای پروژه‌های ESP32 با استفاده از LVGL و EVM Runtime

</div>

## ✨ ویژگی‌های اصلی

| ویژگی | وضعیت | توضیح |
|--------|--------|--------|
| 🎯 طراحی بصری | ✅ کامل | کشیدن و رها کردن ویجت‌ها |
| 🌍 پشتیبانی فارسی | ✅ کامل | متن فارسی، RTL، فونت فارسی |
| 🔌 سازگاری EVM | ✅ کامل | خروجی برای `@native.lvgl` |
| 📱 پیش‌نمایش زنده | ✅ کامل | مشاهده تغییرات بلافاصله |
| 🔄 تولید کد دوگانه | ✅ کامل | هم EVM JS هم کد C |
| 🎨 استایل پیشرفته | ✅ کامل | رنگ، فونت، border، radius |
| ⚡ بهینه‌سازی | ✅ کامل | کد تمیز و بهینه‌شده |

## 🚀 شروع سریع

### پیش‌نیازها
- Node.js 18+
- Git
- مرورگر مدرن

### نصب و راه‌اندازی
```bash
# کلون کردن پروژه
git clone https://github.com/hadipic/-home-shop-electronic-project-esp32-evm-esp32-gui-builder.git

# رفتن به پوشه پروژه
cd evm-esp32-gui-builder

# نصب وابستگی‌ها
npm install

# اجرای سرور توسعه
npm run dev
```

### دسترسی به بیلدر
مرورگر خود را باز کرده و به آدرس زیر بروید:
```
http://localhost:5174
```

## 🎮 راهنمای استفاده

### 1. ایجاد پروژه جدید
- از نوار کناری روی "New Project" کلیک کنید
- اندازه صفحه نمایش را تنظیم کنید (پیش‌فرض: 800x480)
- رنگ تم را انتخاب کنید

### 2. افزودن ویجت
- ویجت‌ها را از پنل سمت چپ بکشید و رها کنید
- ویجت‌های پشتیبانی شده:
  - **Label** (برچسب) - نمایش متن
  - **Button** (دکمه) - قابل کلیک
  - **Arc** (کمان) - نشانگر پیشرفت
  - **Image** (تصویر) - نمایش عکس
  - **Slider** (اسلایدر) - کنترل عددی
  - **Switch** (سوئیچ) - روشن/خاموش
  - **Checkbox** (چک‌باکس) - انتخاب
  - **Text Area** (محیط متن) - ورودی متن
  - **Dropdown** (لیست بازشو) - انتخاب از لیست

### 3. تنظیم خصوصیات
- هر ویجت را انتخاب کنید
- خصوصیات را در پنل سمت راست تنظیم کنید:
  - موقعیت (X, Y)
  - اندازه (عرض، ارتفاع)
  - محتوای متن
  - رنگ‌ها
  - فونت‌ها
  - استایل‌ها

### 4. پشتیبانی از متن فارسی
- متن فارسی را در خصوصیات متن وارد کنید
- جهت‌یابی RTL به صورت خودکار
- فونت فارسی خودکار
- پشتیبانی از Unicode

### 5. تولید کد
- روی دکمه "Generate Code" کلیک کنید
- فرمت خروجی را انتخاب کنید:
  - **EVM JavaScript** (برای ESP32 با EVM)
  - **C Code** (برای LVGL native)

## 📊 ساختار پروژه

```
evm-esp32-gui-builder/
├── src/
│   ├── components/          # کامپوننت‌های Vue
│   │   ├── Builder.vue      # رابط اصلی بیلدر
│   │   ├── WidgetsPanel.vue # پنل ویجت‌ها
│   │   ├── PropertyPanel.vue # پنل خصوصیات
│   │   └── Canvas.vue       # کانvas طراحی
│   ├── router/             # مسیریابی Vue
│   ├── store/              # مدیریت وضعیت
│   ├── utils/              # ابزارهای کمکی
│   │   ├── codeGenerators/ # تولیدکننده کد
│   │   └── widgets/        # تعاریف ویجت‌ها
│   └── main.ts             # نقطه ورود اصلی
├── templates/              # تولیدکننده کد
│   ├── runtimeTemplateEVM.js # تولید کد EVM
│   └── runtimeTemplateC.js   # تولید کد C
├── public/                 # فایل‌های استاتیک
└── package.json            # وابستگی‌ها
```

## 🛠️ تولید کد

### تولید کد EVM JavaScript
کد تولید شده برای EVM-ESP32 کاملاً بهینه شده است:

```javascript
// generated_ui.js
var lv = require('@native.lvgl');
var styleModule = require('@native.lv_style');

function lvgl_lv_main(screen) {
    if (!screen) {
        screen = lv.lv_scr_act();
    }

    // تنظیمات صفحه
    lv.lv_obj_set_x(screen, 0);
    lv.lv_obj_set_y(screen, 0);
    lv.lv_obj_set_width(screen, 800);
    lv.lv_obj_set_height(screen, 480);
    
    styleModule.set_style_bg_color(screen, lv.lv_color_hex(0x000000));

    // ایجاد ویجت‌ها
    var btn1 = lv.lv_btn_create(screen);
    lv.lv_obj_set_x(btn1, 100);
    lv.lv_obj_set_y(btn1, 100);
    lv.lv_obj_set_width(btn1, 120);
    lv.lv_obj_set_height(btn1, 50);
    lv.lv_btn_set_text(btn1, "دکمه فارسی");
    
    // تنظیمات فارسی
    lv.lv_obj_set_persian_font(btn1);
    lv.lv_obj_set_rtl(btn1);
    styleModule.set_style_text_font(btn1, lv.lvgl_style_get_persian_font("persian_16"));
    styleModule.set_style_text_color(btn1, lv.lv_color_hex(0xFFFFFF));
    styleModule.set_style_bg_color(btn1, lv.lv_color_hex(0x0066CC));
    
    // ایجاد آرک
    var arc1 = lv.lv_arc_create(screen);
    lv.lv_obj_set_x(arc1, 300);
    lv.lv_obj_set_y(arc1, 100);
    lv.lv_obj_set_width(arc1, 200);
    lv.lv_obj_set_height(arc1, 200);
    lv.lv_arc_set_value(arc1, 60);
    
    // استایل آرک
    styleModule.lv_arc_hide_knob(arc1);
    styleModule.set_style_arc_width(arc1, 22);
    styleModule.set_style_arc_indic_width(arc1, 22);
    styleModule.set_style_arc_color(arc1, lv.lv_color_hex(0xE3E3E3));
    styleModule.set_style_arc_indic_color(arc1, lv.lv_color_hex(0x589EF8));
    styleModule.set_style_arc_rounded(arc1, false);
}

// اجرای خودکار
lvgl_lv_main();
```

### تولید کد C
```c
// generated_ui.h
#ifndef GENERATED_UI_H
#define GENERATED_UI_H

#include "lvgl.h"

void lvgl_lv_main(lv_obj_t *screen) {
    if (screen == NULL) {
        screen = lv_scr_act();
        lv_obj_remove_style_all(screen);
    }
    
    lv_obj_set_x(screen, 0);
    lv_obj_set_y(screen, 0);
    lv_obj_set_width(screen, 800);
    lv_obj_set_height(screen, 480);
    
    lv_style_t screen_style_MAIN;
    lv_style_init(&screen_style_MAIN);
    lv_style_set_bg_color(&screen_style_MAIN, lv_color_hex(0x000000));
    lv_obj_add_style(screen, &screen_style_MAIN, LV_PART_MAIN);
    
    lv_obj_t *btn1 = lv_btn_create(screen);
    lv_obj_set_x(btn1, 100);
    lv_obj_set_y(btn1, 100);
    lv_obj_set_width(btn1, 120);
    lv_obj_set_height(btn1, 50);
    lv_btn_set_text(btn1, "دکمه فارسی");
}
#endif
```

## 🔌 یکپارچه‌سازی با EVM-ESP32

### نصب روی ESP32
1. پروژه EVM را روی ESP32 نصب کنید:
```bash
git clone https://github.com/hadipic/evm-esp32.git
cd evm-esp32
idf.py build flash monitor
```

2. فایل تولید شده را انتقال دهید:
```bash
# کپی فایل UI تولید شده
cp generated_ui.js /path/to/evm-esp32/data/
```

3. در برنامه اصلی EVM فراخوانی کنید:
```javascript
// main.js در ESP32
var lv = require('@native.lvgl');
var ui = require('./generated_ui');

// راه‌اندازی
lv.lv_init();

// نمایش UI
ui.lvgl_lv_main();
```

### استفاده در خانه هوشمند
```javascript
// مثال کنترل خانه هوشمند
var gpio = require('@native.gpio');
var mqtt = require('@native.mqtt');

// کنترل لامپ از طریق GUI
function light_control(value) {
    gpio.write(23, value); // کنترل پین GPIO
    mqtt.publish("home/lights/room1", value ? "ON" : "OFF");
}

// اتصال به دکمه در UI
var btn_light = lv.lv_btn_create(screen);
lv.lv_btn_set_text(btn_light, "کنترل لامپ");
// اضافه کردن event handler
```

## 📱 ویجت‌های پشتیبانی شده

| ویجت | آیکون | وضعیت | توضیح |
|------|--------|--------|--------|
| **Label** | 🏷️ | ✅ کامل | نمایش متن با پشتیبانی فارسی |
| **Button** | 🔘 | ✅ کامل | دکمه قابل کلیک با استایل |
| **Arc** | 🔵 | ✅ کامل | کمان با knob مخفی و رنگ‌بندی |
| **Image** | 🖼️ | ✅ کامل | نمایش تصویر از حافظه |
| **Slider** | 🎚️ | ✅ کامل | کنترلر عددی با range |
| **Switch** | 🔄 | ✅ کامل | سوئیچ روشن/خاموش |
| **Checkbox** | ☑️ | ✅ کامل | انتخاب چندگانه |
| **Text Area** | 📝 | ✅ کامل | ورودی متن چندخطی |
| **Dropdown** | 📋 | ✅ کامل | لیست انتخاب |

## 🎨 استایل‌های پشتیبانی شده

### برای همه ویجت‌ها:
- `bg_color` - رنگ پس‌زمینه
- `bg_opa` - شفافیت پس‌زمینه
- `text_color` - رنگ متن
- `text_font` - فونت متن
- `border_color` - رنگ border
- `border_width` - ضخامت border
- `radius` - گوشه‌های گرد

### مخصوص آرک:
- `arc_width` - عرض background arc
- `arc_indic_width` - عرض indicator arc
- `arc_color` - رنگ background arc
- `arc_indic_color` - رنگ indicator arc
- `arc_rounded` - گوشه‌های گرد آرک
- `hide_knob` - مخفی کردن knob

## 🌍 پشتیبانی فارسی

### ویژگی‌های فارسی:
- ✅ تشخیص خودکار متن فارسی
- ✅ تنظیم خودکار جهت RTL
- ✅ فونت فارسی خودکار
- ✅ پشتیبانی از Unicode
- ✅ ترکیب حروف فارسی
- ✅ اعداد فارسی

### مثال کد فارسی:
```javascript
// ایجاد برچسب فارسی
var label_fa = lv.lv_label_create(screen);
lv.lv_label_set_text(label_fa, "سلام دنیا");

// تنظیمات خودکار فارسی
lv.lv_obj_set_persian_font(label_fa);  // فونت فارسی
lv.lv_obj_set_rtl(label_fa);           // جهت راست به چپ

// استایل فارسی
styleModule.set_style_text_font(label_fa, lv.lvgl_style_get_persian_font("persian_16"));
styleModule.set_style_text_color(label_fa, lv.lv_color_hex(0xFFFFFF));
```

## 🔧 توسعه

### اجرای محیط توسعه
```bash
# سرور توسعه
npm run dev

# ساخت برای تولید
npm run build

# پیش‌نمایش تولید
npm run preview
```

### تست
```bash
# اجرای تست‌ها
npm test

# تست با coverage
npm run test:coverage
```

### افزودن ویجت جدید
1. فایل ویجت را در `src/utils/widgets/` ایجاد کنید
2. در `src/components/WidgetsPanel.vue` ثبت کنید
3. تابع تولید کد را در `templates/runtimeTemplateEVM.js` اضافه کنید

## 🤝 مشارکت

مشارکت شما باعث پیشرفت پروژه می‌شود:

1. پروژه را Fork کنید
2. شاخه feature ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. تغییرات را commit کنید (`git commit -m 'Add AmazingFeature'`)
4. به شاخه push کنید (`git push origin feature/AmazingFeature`)
5. Pull Request ایجاد کنید

### دستورالعمل مشارکت
- از TypeScript استفاده کنید
- API Composition Vue 3 را رعایت کنید
- تست بنویسید
- مستندات را به روز کنید
- از هر دو زبان فارسی و انگلیسی پشتیبانی کنید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است - جزئیات در فایل [LICENSE](LICENSE) مشاهده کنید.

## 🙏 تقدیر و تشکر

- [LVGL](https://lvgl.io/) - کتابخانه گرافیکی قدرتمند
- [EVM](https://github.com/hadipic/evm-esp32) - ماشین مجازی JavaScript برای ESP32
- [Vue.js](https://vuejs.org/) - فریم‌ورک پیشرو JavaScript
- [Vite](https://vitejs.dev/) - ابزار نسل جدید فرانت‌اند

## 📞 تماس با ما

نگهدارنده پروژه: [hadipic](https://github.com/hadipic)

لینک پروژه: [https://github.com/hadipic/-home-shop-electronic-project-esp32-evm-esp32-gui-builder](https://github.com/hadipic/-home-shop-electronic-project-esp32-evm-esp32-gui-builder)

---

<div dir="rtl" align="right">

## راهنمای فارسی کامل

### 🎯 هدف پروژه
ایجاد یک ابزار طراحی رابط کاربری گرافیکی (GUI) که به کاربران غیرمتخصص امکان طراحی واسط کاربری برای دستگاه‌های ESP32 را می‌دهد.

### 🛠️ نصب و راه‌اندازی

```bash
# 1. دریافت پروژه
git clone https://github.com/hadipic/-home-shop-electronic-project-esp32-evm-esp32-gui-builder.git

# 2. ورود به پوشه پروژه
cd evm-esp32-gui-builder

# 3. نصب وابستگی‌ها
npm install

# 4. اجرای برنامه
npm run dev
```

### 🎨 شروع طراحی
1. مرورگر را باز کرده و به آدرس `http://localhost:5174` بروید
2. روی "پروژه جدید" کلیک کنید
3. اندازه صفحه را تنظیم کنید (پیش‌فرض 800x480 مناسب LCDهای صنعتی است)
4. از پنل سمت چپ ویجت‌ها را بکشید و در صفحه رها کنید

### 📱 تولید کد برای ESP32
1. طراحی خود را کامل کنید
2. روی دکمه "تولید کد" کلیک کنید
3. فرمت "EVM JavaScript" را انتخاب کنید
4. کد تولید شده را کپی کرده و در پروژه ESP32 خود استفاده کنید

### 🔌 اتصال به سخت‌افزار
```javascript
// مثال: کنترل LED از طریق دکمه
var gpio = require('@native.gpio');
gpio.setup(23, gpio.OUTPUT);

var btn = lv.lv_btn_create(screen);
lv.lv_btn_set_text(btn, "روشن کردن LED");

// افزودن event handler
function btn_handler() {
    gpio.write(23, 1); // روشن کردن LED
}
```

### 📚 منابع آموزشی
- [مستندات LVGL](https://docs.lvgl.io/master/)
- [مستندات EVM-ESP32](https://github.com/hadipic/evm-esp32)
- [آموزش Vue.js](https://vuejs.org/guide/introduction.html)

### ❓ پرسش‌های متداول

**سوال:** آیا می‌توانم از فونت‌های فارسی دیگر استفاده کنم؟
**پاسخ:** بله، در قسمت تنظیمات فونت می‌توانید نام فونت فارسی مورد نظر خود را وارد کنید.

**سوال:** چگونه طراحی را روی ESP32 واقعی تست کنم؟
**پاسخ:** کد تولید شده را در پوشه `data` پروژه EVM کپی کرده و ESP32 را flash کنید.

**سوال:** آیا می‌توانم تصاویر خود را اضافه کنم؟
**پاسخ:** بله، تصاویر را به فرمت مناسب تبدیل کرده و در پروژه استفاده کنید.

**سوال:** چگونه با MQtt ارتباط برقرار کنم؟
**پاسخ:** از ماژول `@native.mqtt` در کد تولید شده استفاده کنید.

### 🐛 گزارش مشکل
اگر با مشکلی مواجه شدید:
1. در Issues گیت‌هاب مشکل را گزارش دهید
2. تصاویر و کدهای مربوطه را ضمیمه کنید
3. نسخه‌های نرم‌افزاری خود را ذکر کنید

### 🌟 ویژگی‌های آینده
- [ ] کتابخانه تصاویر
- [ ] انیمیشن‌های پیشرفته
- [ ] پشتیبانی از نمودارها
- [ ] تم‌های آماده
- [ ] export به فرمت‌های دیگر

</div>

---

**⭐ اگر این پروژه را مفید یافتید، لطفاً ستاره دهید!**

[![Star History Chart](https://api.star-history.com/svg?repos=hadipic/-home-shop-electronic-project-esp32-evm-esp32-gui-builder&type=Date)](https://star-history.com/#hadipic/-home-shop-electronic-project-esp32-evm-esp32-gui-builder&Date)
```

## دستورات برای اضافه کردن به گیت‌هاب:

```bash
# اضافه کردن README.md
git add README.md

# کامیت تغییرات
git commit -m "اضافه کردن README.md فارسی/انگلیسی کامل"

# push به گیت‌هاب
git push origin main
```

## همچنین می‌توانید این فایل‌های مفید هم اضافه کنید:

### 1. فایل `CONTRIBUTING.md`:
```markdown
# راهنمای مشارکت

## چگونه مشارکت کنیم؟
1. Issue ایجاد کنید یا یکی را انتخاب کنید
2. Branch جدید ایجاد کنید
3. تغییرات را اعمال کنید
4. تست بنویسید
5. Pull Request ایجاد کنید

## قوانین کدنویسی
- از TypeScript استفاده کنید
- توابع را مستند کنید
- تست واحد بنویسید
- کدهای قدیمی را refactor کنید
```

### 2. فایل `CHANGELOG.md`:
```markdown
# تاریخچه تغییرات

## [1.0.0] - 2024-01-01
### اضافه شده
- طراحی پایه بیلدر
- پشتیبانی از ویجت‌های اصلی
- تولید کد EVM و C
- پشتیبانی فارسی کامل
- سیستم استایل پیشرفته
```
